#ifndef uint
#define uint unsigned int
#endif

class CudaOperator {
    public:
        double *add(double* data);
        double *add(double* data);
        double *add(double* data);
        void setup(int size, double* data);
};